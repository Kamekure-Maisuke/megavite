## セットアップ手順

```bash
npm init vite@latest megavite -- --template vue

npm install -D tailwindcss@latest postcss@latest autoprefixer@latest

npx tailwindcss init -p

npm install vue-router@4 d3js daisyui
```

```bash
# or npm ci
npm install

npm run dev
```
